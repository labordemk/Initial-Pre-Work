//last lesson was about export default. in order to import a default export, you need to use a different import syntax which is the following

import subtract from "math_functions";
"use strict";
subtract(7,4);
