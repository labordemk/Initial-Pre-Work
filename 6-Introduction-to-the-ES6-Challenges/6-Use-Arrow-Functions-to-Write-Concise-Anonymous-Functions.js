//don't need to name functions. instead create inline functions. don't need to name these functions because you don't use them anywhere else.

const magic = () => {
  "use strict";
  return new Date();
};
