//generating random whole numbers. you need to use Math.random to get a random decimal, multiply the random decimal by the number you want it to be between, and then use Math.floor() to round the number down to its nearest whole number

var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.


return Math.floor(Math.random() * 10);
}
