// comparison operators like == which compares two values and returns true if they're equivalent or false if they're not. it is different from = which assigns the value at the right of the operator to a variable in the left

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
